/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NewTodoInputEffectsDirective } from './new-todo-input-effects.directive';

describe('Directive: NewTodoInputEffects', () => {
  it('should create an instance', () => {
    let directive = new NewTodoInputEffectsDirective();
    expect(directive).toBeTruthy();
  });
});
