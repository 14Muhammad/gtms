/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NewTodoRecordButtonDirective } from './new-todo-record-button.directive';

describe('Directive: NewTodoRecordButton', () => {
  it('should create an instance', () => {
    let directive = new NewTodoRecordButtonDirective();
    expect(directive).toBeTruthy();
  });
});
