/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NewTodoInputFieldDirective } from './new-todo-input-field.directive';

describe('Directive: NewTodoInputField', () => {
  it('should create an instance', () => {
    let directive = new NewTodoInputFieldDirective();
    expect(directive).toBeTruthy();
  });
});
