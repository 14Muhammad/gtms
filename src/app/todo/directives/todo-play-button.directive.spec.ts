/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TodoPlayButtonDirective } from './todo-play-button.directive';

describe('Directive: TodoPlayButton', () => {
  it('should create an instance', () => {
    let directive = new TodoPlayButtonDirective();
    expect(directive).toBeTruthy();
  });
});
