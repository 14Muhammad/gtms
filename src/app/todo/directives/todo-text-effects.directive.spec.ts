/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TodoTextEffectsDirective } from './todo-text-effects.directive';

describe('Directive: TodoTextEffects', () => {
  it('should create an instance', () => {
    let directive = new TodoTextEffectsDirective();
    expect(directive).toBeTruthy();
  });
});
